# V0-003 — Create Domain Types

## Objective

Model the inputs, rules, and outputs needed by the calculation.

## Prerequisites

V0-002 is complete.

## Subtasks

- Add `FuelType` and `ThresholdSource`.
- Add `FuelPrice` and `VehicleEfficiency`.
- Add `FuelCalculationInput` and `FuelCalculationResult`.
- Keep the types independent of Flutter.

## Expected files

- `lib/.../domain/`.
- `docs/architecture/domain-model.md`.

## Acceptance criteria

Domain types express valid calculation inputs, rule origin, and all result values without UI dependencies.

## Tests

Compile the domain and add construction/equality tests where appropriate.

## Out of scope

Persistence and Riverpod state.
