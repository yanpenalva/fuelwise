# V1-001 — Add Dependencies

## Objective

Add the V1 persistence and state-management toolchain.

## Prerequisites

V0 is functional and tested.

## Subtasks

- Add Riverpod.
- Add Drift, SQLite integration, `build_runner`, and `drift_dev`.
- Add `SharedPreferencesAsync`.
- Configure code generation.

## Expected files

- `pubspec.yaml`.
- `build.yaml` if required.

## Acceptance criteria

Dependencies resolve and generated-code commands are documented and repeatable.

## Tests

Run dependency resolution, generation, analysis, and the V0 suite.

## Out of scope

New product behavior.
